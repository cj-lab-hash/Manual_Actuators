
# Replace with your External connection string from Render Postgres for local dev
# Example:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=

# Optional: adjust pool size (keep small on Render)
# PGPOOL_MAX=5
